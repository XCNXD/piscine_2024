// int	array[9][9];
int main(int argc, char **argv)
{
	if (argc == 2)
	{
		int str = argv[1];
		int array = {}
		int i = 0;
		
		// while (str[i] != 'c' || str[i] != 'r' || str[i] < '1' || str[i] > '9')
		// 	i++;
		// while (str[i] != 0)
		// {
		// 	i++;
		// }

		// handle normal case
	}
}